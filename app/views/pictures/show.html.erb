<h1>Picturesshow</h1>
<p>Find me in app/views/pictures/show.html.erb</p>



<%= image_tag (@picture.image_url), style:"height: 40%; width: 40%;"  %>

<%= link_to @tag.name %>

<%=form_for "/pictures" do |f| %>
    <%= f.label :comment %>
    <%=f.text_field :name%>
    <%= f.submit "Add Comment"%>
<%end%>

<%= button_to "Delete comment", picture_path(@picture), method: "delete" %>
